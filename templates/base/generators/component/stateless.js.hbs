/**
*
* {{ properCase name }}
*
*/

// Node_Module Imports
import React, { ComponentType } from 'react';
import styled from 'styled-components';

// Relative imports
import styles, { CompRoot } from './{{ properCase name }}.style';

const {{ properCase name }} = ({ className, children }) => <CompRoot className={className} data-testid='CompRoot'>
  {children}</CompRoot>;

{{ properCase name }}.defaultProps = {};

const Styled{{ properCase name }} = styled({{ properCase name }})`
${styles};
`;

export default Styled{{ properCase name }};

export { {{ properCase name }} };