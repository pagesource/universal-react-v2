/**
 *
 * {{ properCase name }}
 *
 */
// @flow

// Node_Module Imports
import React, { ComponentType } from 'react';
import styled from 'styled-components';

// Relative imports
import styles from './{{ properCase name }}.style';

const {{ properCase name }} = ({ className, children }) => <div className={className} data-testid='AppContainer'>{children}</div>;

{{ properCase name }}.defaultProps = {};

const Styled{{ properCase name }} = styled({{ properCase name }})`
  ${styles};
`;

export default Styled{{ properCase name }};

export { {{ properCase name }} };
