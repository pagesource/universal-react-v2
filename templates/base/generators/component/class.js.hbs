/**
 *
 * {{ properCase name }}
 */
// @flow

// Node_Module imports
import React, { PureComponent, ComponentType } from 'react';
import styled from 'styled-components';

// Relative imports
import styles, { AppContainer } from './{{ properCase name}}.style';

class {{ properCase name }} extends PureComponent {
  static defaultProps = {};
  render() {
    const {className} = this.props;
    return <AppContainer className={className} data-testid='AppContainer' />;
  }
}

const Styled{{ properCase name }} = styled({{ properCase name }})`
  ${styles};
`;

export default Styled{{ properCase name }};

export { {{ properCase name }} };
