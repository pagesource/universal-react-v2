/**
 *
 * {{ properCase name }}
 *
 */
// @flow

// Node_Module Imports
import React, { ComponentType } from 'react';
import type { Node } from 'react';
import styled from 'styled-components';

// Relative imports
import type { Props } from './types';
import styles from './{{ properCase name }}.style';

const {{ properCase name }} = ({ className, children }: Props): Node => <div className={className}>{children}</div>;

{{ properCase name }}.defaultProps = {};

const Styled{{ properCase name }}: ComponentType<Props> = styled({{ properCase name }})`
  ${styles};
`;

export default Styled{{ properCase name }};

export { {{ properCase name }} };
