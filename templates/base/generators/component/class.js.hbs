/**
*
* {{ properCase name }}
*/

// Node_Module imports
import React, { PureComponent, ComponentType } from 'react';
import styled from 'styled-components';

// Relative imports
import styles, { CompRoot } from './{{ properCase name}}.style';

class {{ properCase name }} extends PureComponent {
static defaultProps = {};
render() {
const {className} = this.props;
return
<CompRoot className={className} data-testid='CompRoot' />;
}
}

const Styled{{ properCase name }} = styled({{ properCase name }})`
${styles};
`;

export default Styled{{ properCase name }};

export { {{ properCase name }} };