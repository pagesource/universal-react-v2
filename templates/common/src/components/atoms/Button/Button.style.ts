import {styled} from '../../../../stiches.config';

export const AppContainer = styled('button', {
  fontSize: '1em',
  margin: '1em',
  padding: '0.25em 1em',
  border: '2px solid black',
  borderRadius: '3px',
  display: 'block',
});