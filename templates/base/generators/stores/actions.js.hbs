import { UPDATE_{{constantCase name}} } from './constants';

export const update{{properCase name}} = (payload, dispatch) => {
  return dispatch({
    type: UPDATE_{{constantCase name}},
    payload
  });
};
