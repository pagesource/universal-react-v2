{
  "name": "base",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build dev",
    "start": "next start dev",
    "storybook:run": "cd storybook && yarn install && yarn storybook",
    "storybook:build": "cd storybook && yarn install && yarn build-storybook"
  },
  "dependencies": {
    "next": "9.3.6",
    "react": "16.13.1",
    "react-dom": "16.13.1"
  }
}
